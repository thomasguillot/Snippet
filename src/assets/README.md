# App icon

Place **`app-icon.png`** here (1024×1024 px recommended). It is used to generate the macOS `.icns` with all required sizes.

- Run `npm run generate-icon` to produce `dist/app.icns`.
- The Electron build runs this step automatically before packaging.
