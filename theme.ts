/**
 * Shared theme values for the Electron main process (window chrome).
 * These should match the CSS variables in src/style.scss:
 * - windowBackground → --color-bg-page
 * - windowSymbolColor → --color-text-heading (or a suitable light color for title bar icons)
 */
export const windowBackground = '#0a0a0a';
export const windowSymbolColor = '#ffffff';
